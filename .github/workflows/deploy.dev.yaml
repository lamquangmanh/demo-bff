name: CD Pipeline

on:
  workflow_run:
    workflows: ['CI Docker Pipeline']
    types:
      - completed

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Pull Docker image
        run: docker pull tahinguyen/core-bff:latest

      - name: Delete Old docker container
        run: docker rm -f core-bff-ctn || true

      - name: Run Docker Container
        env:
          PORT: ${{secrets.PORT}}
        run: |
          docker run -d -p $PORT:8888 --restart unless-stopped --env-file ~/core-fcm-team/.env --name core-bff-ctn tahinguyen/core-bff:latest
